name: package_info_plus_example
description: "watchOS example for package_info_plus_watchos."
publish_to: 'none'
version: 1.2.3+4

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  package_info_plus: any
  package_info_plus_watchos:
    path: ../
  clock: "^1.1.1"
  http: ">=0.13.5 <2.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  device_info_plus: "^13.2.0"

flutter:
  uses-material-design: true
