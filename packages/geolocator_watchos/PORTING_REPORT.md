# geolocator_watchos — porting report

## Verification status

| Aspect | Result |
|---|---|
| Implementation | ✅ Working (CoreLocation FFI) |
| watchOS capability | Full — query, permission, and position stream |
| Host unit tests (`flutter-watchos test`) | ✅ pass |
| Upstream integration test | ○ none upstream (Baseflow manual demo) — verified by building + running the example on-sim |
| Unified demo (`demo/plugins_demo`) | ✅ included |

Marking: ✅ full / passes · ◐ partial — reason given · ○ not applicable (no upstream test) · ✗ unsupported on watchOS.

> This scaffold has been **finished by hand** into a working FFI package; see
> the status above. The generated notes below are kept for provenance.

Generated by `flutter-watchos plugin port` on 2026-07-14.

Source: `geolocator_apple` 2.3.14 (path: `/Users/aliustaoglu/.pub-cache/hosted/pub.dev/geolocator_apple-2.3.14`)
Base platform: ios (Objective-C)
Output: `./geolocator_watchos`

## This is an FFI scaffold

Method-channel plugins are not supported on watchOS — a `pluginClass:`-only implementation builds, but its channel calls throw `MissingPluginException`. The supported plugin model is **dart:ffi**, so this package is an FFI scaffold, not a copy of the source plugin's native code:

- `watchos/Classes/geolocator_watchos_ffi.{h,m}` — the C functions to implement (one example symbol is provided so the package builds and links immediately).
- `watchos/Package.swift` — the FFI manifest; add the frameworks your C code links.
- `lib/geolocator_watchos.dart` — the Dart class over the platform interface; resolve each C symbol via `DynamicLibrary.process()` and override the interface methods.
- `pubspec.yaml` — declares `ffiPlugin: true` and lists your exported symbols under `ffiSymbols` (the CLI force-references each so it survives the static link).

## APIs the source plugin used

### Not available on watchOS

These iOS/macOS APIs have **no watchOS equivalent** — the capability must be omitted or redesigned (often via the paired iPhone):

| API | Used at | Why / watchOS note |
|---|---|---|
| BackgroundFetch | `darwin/geolocator_apple/Sources/geolocator_apple/Handlers/GeolocationHandler.m:207` | BackgroundTasks (BGTaskScheduler) is an iOS API. watchOS has its own background model: scheduleBackgroundRefresh on WKApplication, delivering `WKRefreshBackgroundTask` subclasses to the extension delegate. Porting requires rethinking the scheduling contract, not a mechanical rename. |
| UIApplication | `darwin/geolocator_apple/Sources/geolocator_apple/GeolocatorPlugin.m:178` | UIApplication does not exist on watchOS — the app object is WKApplication (watchOS 7+) / WKExtension. For opening URLs, watchOS only supports `WKExtension.shared().openSystemURL(_:)` for tel: and sms: schemes; generic URL launching has no watchOS equivalent. |

### Available, but review

These work on watchOS (often differently than iOS) — implement them, checking the note:

| API | Used at | watchOS note |
|---|---|---|
| CoreLocation | `darwin/geolocator_apple/Sources/geolocator_apple/GeolocatorPlugin.m:129` (+43 more) | CoreLocation IS available on watchOS: requestLocation, continuous updates, and heading (watchOS 6+) work, with GPS on cellular/GPS models and phone-assisted fixes otherwise. Differences to review: authorization is shared with the paired iPhone app when one exists, region monitoring is limited, and background location needs a workout or background-refresh context. |

## Checklist

- [x] Declare your C functions in `watchos/Classes/geolocator_watchos_ffi.h` and implement them in the `.m`, one per platform-interface method you support.
- [x] List every exported symbol under `ffiSymbols` in `pubspec.yaml`, and the frameworks you link in `Package.swift`.
- [x] Add a `lookupFunction` binding per symbol in the Dart `Bindings` class and override the platform-interface methods.
- [x] Add the package to a watchOS app (`flutter-watchos create` one if needed), build for `watchsimulator`, then `nm` the binary to confirm your `ffiSymbols` are present (type `T`).
- [x] Bump the version and update `CHANGELOG.md` before publishing.

---

See the finished plugins in the `flutterwatch/plugins` repo (e.g. `path_provider_watchos`, `battery_plus_watchos`) for worked examples of this exact shape.
