name: local_auth_example
description: "watchOS example for local_auth_watchos."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  local_auth: any
  local_auth_watchos:
    path: ../
  local_auth_android: ^2.0.0
  local_auth_darwin: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
