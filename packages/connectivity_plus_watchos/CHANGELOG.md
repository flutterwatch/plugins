## 0.1.0

* First working release. FFI implementation over the Network framework's
  `NWPathMonitor`; poll-based `onConnectivityChanged`.

## 0.0.1

* Initial watchOS scaffolding generated by `flutter-watchos plugin port` from
  `connectivity_plus`.
