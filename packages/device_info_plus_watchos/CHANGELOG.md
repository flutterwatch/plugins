## 0.1.0

* First working release. FFI implementation over `WKInterfaceDevice`,
  `uname()`, `NSProcessInfo`, and `NSFileManager`, shaped for
  `IosDeviceInfo`.

## 0.0.1

* Initial watchOS scaffolding generated by `flutter-watchos plugin port` from
  `device_info_plus`.
