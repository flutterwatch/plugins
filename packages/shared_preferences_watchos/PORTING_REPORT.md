# shared_preferences_watchos — porting report

Scaffolded by `flutter-watchos plugin port --from-pub shared_preferences_foundation`
(Swift + Pigeon method-channel plugin), then rewritten by hand as an FFI
implementation — method-channel plugins are not supported on watchOS.

## Status

✅ WORKING FFI implementation:

- `watchos/Classes/shared_preferences_watchos_ffi.m` persists the whole
  store as one JSON string in `NSUserDefaults` and exposes three symbols
  (`load` / `save` / `free`). All symbols carry `used` +
  default-visibility and `ffiSymbols` forced references, so they survive
  the static link.
- `lib/src/watchos_prefs_store.dart` is the Dart-side store; typing and
  filtering live here so JSON round-trips each value type exactly.
- `lib/shared_preferences_watchos.dart` registers BOTH platform interfaces
  (legacy `SharedPreferencesStorePlatform` and async
  `SharedPreferencesAsyncPlatform`) against that store, mirroring
  `shared_preferences_foundation`.

## Notes

- The single-blob design isolates the plugin's data from the system's
  `NSUserDefaults` keys, so `getAll` / `getKeys` never have to guess which
  keys are ours.
- The legacy store defaults to the `flutter.` key prefix (as the iOS impl
  does); the async store uses raw keys with an optional allowList filter.

---

Generated by `flutter-watchos plugin port`, finished by hand. FFI federated
model — no method channels, no Flutter patching.
