## 0.1.0

* First working release. FFI implementation over `NSUserDefaults`
  (single JSON blob); registers both the legacy and async platform
  interfaces.

## 0.0.1

* Initial watchOS scaffolding generated by `flutter-watchos plugin port` from
  `shared_preferences_foundation`.
